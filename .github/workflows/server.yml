name: server
on:
  workflow_dispatch:

jobs:
  minecraft:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Download Paper 1.16.5
        run: |
          echo "=== Downloading Paper 1.16.5 (build 794) ==="
          curl -L -o server.jar \
            https://api.papermc.io/v2/projects/paper/versions/1.16.5/builds/794/downloads/paper-1.16.5-794.jar
          echo "✔ server.jar ready"

      - name: Install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libjemalloc2 net-tools

      - name: Setup Playit Tunnel (FIRST TIME - Check logs for claim URL!)
        run: |
          echo "=== Downloading Playit ==="
          wget -q https://github.com/playit-cloud/playit-agent/releases/latest/download/playit-linux-amd64
          chmod +x playit-linux-amd64
          
          echo "=== Starting Playit - CHECK LOGS FOR CLAIM URL! ==="
          ./playit-linux-amd64 > playit.log 2>&1 &
          PLAYIT_PID=$!
          
          echo "⏳ Waiting 30 seconds for playit to start..."
          sleep 30
          
          echo "=== PLAYIT LOGS (Contains claim URL) ==="
          cat playit.log
          echo "================================"
          
          echo "⏰ WAITING 8 MINUTES for you to claim the tunnel!"
          echo "👆 Find the claim URL above and open it in browser"
          sleep 480
          
          echo "=== Final Playit Logs ==="
          cat playit.log
          
          kill $PLAYIT_PID 2>/dev/null || true

      - name: One-step server (Java 21) → 10 min → graceful stop
        run: |
          export MALLOC_ARENA_MAX=2
          export MALLOC_MMAP_THRESHOLD_=131072
          export MALLOC_TRIM_THRESHOLD_=131072
          export MALLOC_TOP_PAD_=131072
          export MALLOC_TEMP_THRESHOLD_=0
          export MALLOC_ARENA_TESTRATE=4
          export LD_PRELOAD=/usr/lib/x86_64-linux-gnu/libjemalloc.so.2

          test -f eula.txt || echo "eula=true" > eula.txt

          java -DPaper.IgnoreJavaVersion=true \
            -Xmx16G -Xms12G \
            -XX:+UseG1GC -XX:MaxGCPauseMillis=200 \
            -XX:+UnlockExperimentalVMOptions -XX:+DisableExplicitGC \
            -XX:G1NewSizePercent=30 -XX:G1MaxNewSizePercent=40 \
            -XX:G1HeapRegionSize=8M -XX:G1ReservePercent=20 \
            -XX:G1HeapWastePercent=5 \
            -jar server.jar --nogui &
          SERVER_PID=$!
          echo "✔ Server started (PID: $SERVER_PID)"

          sleep 600        # 10 دقائق
          echo "=== Graceful stop ==="
          kill -TERM $SERVER_PID
          wait $SERVER_PID || true
          echo "✔ Server stopped"

      - name: Save Playit configuration
        run: |
          git config user.name  "github-actions"
          git config user.email "actions@github.com"
          
          echo "=== Saving Playit files ==="
          git add playit.toml playit-linux-amd64 playit.log 2>/dev/null || true
          
          if ! git diff --cached --quiet 2>/dev/null; then
            git commit -m "Save Playit config $(date -u)"
            git pull --rebase --autostash \
              https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}.git
            git push https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}.git
            echo "✔ Playit config saved"
          else
            echo "ℹ No Playit changes to save"
          fi

      - name: Save world+plugins if changed
        run: |
          git config user.name  "github-actions"
          git config user.email "actions@github.com"
          git add world plugins playit.toml playit-linux-amd64 playit.log || true
          if ! git diff --cached --quiet; then
            git commit -m "Auto-save $(date -u)"
            git pull --rebase --autostash \
              https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}.git
            git push https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}.git
          fi

      - name: Retrigger if alone
        env:
          GH_TOKEN: ${{ secrets.PAT }}
        run: |
          runs=$(gh run list -w server.yml --json status -q \
               '.[]|select(.status=="queued" or .status=="in_progress")|length')
          if [ "$runs" -le 1 ] && [ "${{ github.run_attempt }}" -lt 3 ]; then
            gh workflow run server.yml
          fi
